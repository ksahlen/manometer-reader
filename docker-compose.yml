services:
  manometer-reader:
    build: .
    container_name: manometer-reader
    restart: unless-stopped
    volumes:
      - ./service/config.local.yaml:/app/config.yaml:ro
      - debug-images:/tmp/manometer-debug
    network_mode: host   # Simplest for mDNS/.local resolution

volumes:
  debug-images:
